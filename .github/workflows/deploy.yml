name: Deploy Matching Board v2 Theme to ConoHa WING

on:
  push:
    branches: [ main ]      # mainブランチ更新で自動デプロイ
  workflow_dispatch:         # 手動デプロイも可能

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 🧾 コードチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🔐 SSHセットアップ
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p 8022 -H www1012.conoha.ne.jp >> ~/.ssh/known_hosts

      # 🔍 接続テスト（GitHub Actions → ConoHa）
      - name: Test SSH connection
        run: |
          ssh -i ~/.ssh/id_ed25519 -p 8022 c3659606@www1012.conoha.ne.jp "echo '✅ SSH connection successful!' && pwd"

      # 🚀 デプロイ（SFTP経由）
      - name: Deploy Matching Board Theme files
        uses: wlixcc/SFTP-Deploy-Action@v1.2.6
        with:
          server: www1012.conoha.ne.jp
          port: 8022
          username: c3659606
          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
          local_path: ./                        # ← GitHubリポジトリ直下にテーマファイルがある場合
          remote_path: "public_html/2000km.net/wp-content/themes/matching-board-v2/"  # ← デプロイ先
          sftp_only: true
          delete_remote_files: false
          rsyncArgs: "--exclude='.git/' --exclude='.github/' --exclude='node_modules/' --exclude='.env' --exclude='.DS_Store'"
